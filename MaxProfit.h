#ifndef MAXPROFIT_H
#define MAXPROFIT_H

#include <vector>
#include <string>
#include <iostream>
#include <fstream>
#include <math.h>
#include <stdio.h>
#include <chrono>

class MaxProfit {
private:
    std::vector<std::string> cardNames;
    std::vector<int> cardPrices;
    // should always be same length, indeces match cards with prices
public:


};

#endif
